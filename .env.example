# ⚠️ IMPORTANT: Never commit the actual .env file!
# Copy this file to .env and fill in your real credentials
# The .env file is in .gitignore and will never be pushed to GitHub

# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-a-random-string
FLASK_ENV=production
PORT=5000

# CORS - Add your frontend domain
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:3000

# Google OAuth (Get from: https://console.cloud.google.com)
# Leave empty to disable Google login
GOOGLE_CLIENT_ID=your-google-client-id-here

# Email Configuration (for password reset)
# Get app password from: https://myaccount.google.com/apppasswords
# Leave empty to disable password reset emails
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Database (SQLite - no configuration needed)
# The app will create career_guidance.db automatically
