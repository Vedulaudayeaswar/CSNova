# Flask and Web Server
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0
Flask-Limiter==3.5.0
python-dotenv==1.0.0

# Google OAuth
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# Database - SQLite is built into Python
# No additional packages needed!

# NOTE: Face detection uses TensorFlow.js (client-side)
# NOTE: Career guidance uses rule-based matching (no ML needed)
